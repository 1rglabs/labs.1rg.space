// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this to the production URL to enable canonical URLs, RSS, and sitemaps.
  // site: 'https://labs.1rg.coop',
});
